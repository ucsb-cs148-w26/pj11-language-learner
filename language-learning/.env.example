# In your terminal, write "cp .env.example .env.local"
# In your new .env.local file, replace the placeholder values with your actual Supabase project URL and anon key.
# Use the Publishable key, not the Anon key, for NEXT_PUBLIC_SUPABASE_ANON_KEY.

NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key